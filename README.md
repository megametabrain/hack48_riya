
  # Customize UI with DojoBuild

  This is a code bundle for Customize UI with DojoBuild. The original project is available at https://www.figma.com/design/zHfA3cucMWhocjLaNt3S9z/Customize-UI-with-DojoBuild.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  